---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: accessrequests.netwatch.vtk.io
spec:
  group: netwatch.vtk.io
  names:
    kind: AccessRequest
    listKind: AccessRequestList
    plural: accessrequests
    shortNames:
    - ar
    singular: accessrequest
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessRequestSpec defines the desired state of AccessRequest
            properties:
              cidr:
                type: string
              description:
                type: string
              direction:
                type: string
              duration:
                format: int64
                type: integer
              ports:
                type: string
              requestID:
                description: RequestID is the unique ID shared by the final Access
                  objects, generated at submission time.
                type: string
              requestType:
                type: string
              requestor:
                type: string
              service:
                type: string
              sourceCloneName:
                description: SourceCloneName is the name of the service clone created
                  in the source namespace.
                type: string
              sourceService:
                type: string
              status:
                description: |-
                  Status indicates the current state of the request.
                  Can be "PendingFull", "PendingTarget", "PendingSource".
                type: string
              targetCloneName:
                description: TargetCloneName is the name of the service clone created
                  in the target namespace.
                type: string
              targetService:
                type: string
            required:
            - direction
            - duration
            - ports
            - requestType
            - requestor
            - status
            type: object
          status:
            description: AccessRequestStatus defines the observed state of AccessRequest
            properties:
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
